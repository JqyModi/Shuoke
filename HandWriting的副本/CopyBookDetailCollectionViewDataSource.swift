//
//  CopyBookDetailCollectionViewDataSource.swift
//  HandWriting
//
//  Created by mac on 17/8/25.
//  Copyright © 2017年 modi. All rights reserved.
//

import Foundation
import BothamUI

class CopyBookDetailCollectionViewDataSource: BothamCollectionViewDataSource<CopyBookDetail, CopyBookDetailCollectionViewCell> {
    
//    var copyBookHeader: Common?
    /*
    func collectionView(_ collectionView: UICollectionView,
                        viewForSupplementaryElementOfKind kind: String,
                        atIndexPath indexPath: NSIndexPath) -> UICollectionReusableView {
        /*
        if kind == UICollectionElementKindSectionHeader {
            let headerView =
                collectionView.dequeueReusableSupplementaryView(ofKind: kind,
                                                                withReuseIdentifier: "CopyBookDetailCollectionHeaderViewReusableIdentifier",
                                                                for: indexPath as IndexPath)
                    as! CopyBookDetailCollectionHeaderView
            if let header = self.copyBookHeader {
                headerView.configure(forItem: header)
            }
            return headerView
            
        }
         */
        assert(false, "Unexpected element kind")
    }
     */
    
}
